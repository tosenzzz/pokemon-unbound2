{
  "name": "pokemon",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "yarn --cwd ../pokemon-unbound/frontend build && yarn dep",
    "dep": "gh-pages -d dist"
  },
  "dependencies": {
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
